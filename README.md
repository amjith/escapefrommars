# Escape From Mars 2

A browser-based sci-fi action game built with Phaser 3 and vanilla JavaScript.

## Quick Start
Build a deployable bundle:
```bash
mkdir -p bundle
cp -R index.html src assets bundle/
```

Serve locally:
```bash
python3 -m http.server 8000
```

Open `http://localhost:8000` in your browser.
